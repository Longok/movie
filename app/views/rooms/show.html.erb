<div>
    <h1><%= @room.name %></h1>
    <h4 class="text-center">The room has <%= pluralize @room.seats.count, "seat" %></h4>
    <h4 class="text-center">The film has <%= pluralize @room.showtimes.count, "showtime" %></h4>
</div>
<%= link_to "Create Seat", new_room_seat_path(@room) %> <br>
<div>
    Lịch chiếu:
    <br/>
    <% @room.showtimes.each do |showtime| %>
        <%= link_to showtime.show_at, showtime_path(showtime) %> |
    <% end %>
</div>
<br/>
Chỗ ngồi:
<div class="col-8 col-offset-3">
    <div class="grid-container">
        <% @room.seats.each do |seat| %>
            <div class="grid-item">
                <%= link_to seat.name, edit_room_seat_path(seat.room, seat) %>
                   
            </div>
        <% end %>
    </div>
</div>